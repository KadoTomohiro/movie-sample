import { Routes } from '@angular/router';
import {SignageComponent} from './../components';

export const Routes: Routes = [
    { path: '',      component: SignageComponent },
    { path: 'signage',      component: SignageComponent },
    // { path: ':id',      component: Home },
];